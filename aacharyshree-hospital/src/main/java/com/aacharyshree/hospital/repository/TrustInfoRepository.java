package com.aacharyshree.hospital.repository;

import com.aacharyshree.hospital.entity.TrustInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrustInfoRepository extends JpaRepository<TrustInfo, Long> {
}
