import CollectionManager from "../components/collection/CollectionManager";
import { entityConfigs } from "../config/entityConfigs";
export default function Events() { return <CollectionManager config={entityConfigs.events} />; }
